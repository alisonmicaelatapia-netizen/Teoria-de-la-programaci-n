# CONTENIDOS DE LA UNIDAD
# 🔀 Modularidad
Es un paradigma de la programación la cual consiste en dividir un sistema en partes más pequeñas e independientes, llamadas módulos, que interactúan entre sí, esto se lo puede aplicar cuando se tiene algoritmos largos y complejos. 
* **if Simple:** Ejecuta un bloque de código que si una condición es verdadera. Si es falsa no se ejecuta.
> **Ejemplo 1:**
